kind: Pod
apiVersion: v1
metadata:
  name: nginx
  labels:
    name: nginx
  namespace: default
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
      <%# Variables can be accessed by the name easily %>
      - containerPort: <%= port.number %>
        name: <%= port.name %>
