kind: Pod
apiVersion: v1
metadata:
  name: nginx
  labels:
    name: nginx
  namespace: default
spec:
  containers:
  - name: nginx
    image: nginx:latest
    ports:
      <% var.ports.each { |port| %>
      - containerPort: <%= port.number %>
        name: <%= port.name %>
      <% } %>
